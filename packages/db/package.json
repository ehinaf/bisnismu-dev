{
  "name": "@bisnismu/db",
  "version": "0.0.1",
  "private": true,
  "description": "Prisma schema + client BisnisMu",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.json",
    "test": "echo \"@bisnismu/db: no tests yet\" && exit 0",
    "lint": "echo \"@bisnismu/db: no lint config yet\" && exit 0",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7.8.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0"
  }
}
