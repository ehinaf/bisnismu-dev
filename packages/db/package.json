{
  "name": "@bisnismu/db",
  "version": "0.0.1",
  "private": true,
  "description": "Prisma schema + client BisnisMu",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "prisma generate",
    "test": "echo \"@bisnismu/db: no tests yet\" && exit 0",
    "lint": "echo \"@bisnismu/db: no lint config yet\" && exit 0",
    "typecheck": "echo \"@bisnismu/db: no tsconfig yet\" && exit 0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7.8.0"
  }
}
