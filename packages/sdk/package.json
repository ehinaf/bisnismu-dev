{
  "name": "@bisnismu/sdk",
  "version": "0.0.1",
  "private": true,
  "description": "Client SDK untuk memanggil API BisnisMu",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "echo \"@bisnismu/sdk: belum ada build step\" && exit 0",
    "test": "echo \"@bisnismu/sdk: no tests yet\" && exit 0",
    "lint": "echo \"@bisnismu/sdk: no lint config yet\" && exit 0",
    "typecheck": "echo \"@bisnismu/sdk: no tsconfig yet\" && exit 0"
  },
  "dependencies": {
    "@bisnismu/types": "workspace:*"
  }
}
