{
  "name": "@bisnismu/web",
  "version": "0.0.1",
  "private": true,
  "description": "BisnisMu frontend (Next.js)",
  "scripts": {
    "dev": "echo \"@bisnismu/web: Next.js belum di-scaffold\" && exit 0",
    "build": "echo \"@bisnismu/web: Next.js belum di-scaffold\" && exit 0",
    "test": "echo \"@bisnismu/web: no tests yet\" && exit 0",
    "lint": "echo \"@bisnismu/web: no lint config yet\" && exit 0",
    "typecheck": "echo \"@bisnismu/web: no tsconfig yet\" && exit 0"
  },
  "dependencies": {
    "@bisnismu/sdk": "workspace:*",
    "@bisnismu/types": "workspace:*",
    "@bisnismu/ui": "workspace:*"
  }
}
